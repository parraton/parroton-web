export interface JettonMetadata {
  decimals: string;
  description: string;
  image: string;
  name: string;
  social: string[];
  symbol: string;
  websites: string[];
}

export type Maybe<T> = T | null | undefined;
