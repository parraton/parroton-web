/* eslint-disable no-unused-vars */

export enum Actions {
  deposit = 'deposit',
  withdraw = 'withdraw',
  faucet = 'faucet',
  claim = 'claim',
}

export enum TransactionStatus {
  Pending = 'pending',
  Success = 'success',
  Error = 'error',
  Hash = 'hash',
}
